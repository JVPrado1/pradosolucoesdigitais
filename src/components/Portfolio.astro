---
// Portfolio moderno com glassmorphism e carrossel mobile
---

<section id="portfolio" class="py-16 relative overflow-hidden bg-[#121212]">
  <!-- Background Pattern -->
  <div class="absolute inset-0 opacity-20">
    <div class="absolute inset-0" style="background-image: radial-gradient(circle at 1px 1px, rgba(255,255,255,0.1) 1px, transparent 0); background-size: 50px 50px;"></div>
  </div>

  <!-- Floating Elements -->
  <div class="absolute inset-0 overflow-hidden">
    <div class="floating-element absolute top-20 left-10 w-2 h-2 bg-[#00B050] rounded-full opacity-40"></div>
    <div class="floating-element absolute top-40 right-20 w-1 h-1 bg-white rounded-full opacity-30"></div>
    <div class="floating-element absolute bottom-40 left-20 w-1.5 h-1.5 bg-[#00B050] rounded-full opacity-35"></div>
    <div class="floating-element absolute bottom-20 right-40 w-1 h-1 bg-white rounded-full opacity-25"></div>
  </div>

  <div class="relative z-10 max-w-6xl mx-auto px-6 lg:px-8">
    <!-- Header -->
    <div class="text-center mb-12 md:mb-20">
      <h2 class="text-4xl md:text-5xl font-bold mb-8 leading-tight">
        <span class="text-white">Projetos </span>
        <span class="bg-gradient-to-r from-green-400 to-blue-400 bg-clip-text text-transparent">em destaque</span>
      </h2>
      <p class="text-lg md:text-xl text-white/70 max-w-4xl mx-auto leading-relaxed">
        Conheça alguns dos projetos que desenvolvemos, entregando soluções personalizadas e resultados eficientes para nossos clientes.
      </p>
    </div>

    <!-- Mobile Carousel -->
    <div class="md:hidden relative">
      <div id="portfolio-carousel" class="overflow-hidden">
        <div id="portfolio-carousel-inner" class="flex transition-transform duration-300">
          <!-- Cards serão inseridos via JS -->
        </div>
      </div>
      <div class="flex flex-col items-center mt-6">
        <div id="portfolio-carousel-dots" class="flex space-x-2 mb-2">
          <!-- Dots serão inseridos via JS -->
        </div>
        <p class="text-sm text-white/60">Arraste para ver os projetos</p>
      </div>
    </div>

    <!-- Desktop Grid -->
    <div class="hidden md:grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 items-stretch">
        <!-- Projeto 1: Track By Track -->
        <div class="group bg-white/5 backdrop-blur-sm rounded-2xl border border-white/10 shadow-2xl hover:shadow-[#00B050]/10 transition-all duration-500 hover:scale-105 hover:border-[#00B050]/30 overflow-hidden flex flex-col h-full">
            <div class="relative h-44 bg-[#00B050]/10 flex-shrink-0">
                <img src="/trackbytrack.webp" alt="Track By Track" class="w-full h-full object-cover opacity-80 group-hover:opacity-100 transition-opacity duration-300">
                <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-transparent"></div>
                <div class="absolute top-4 right-4">
                    <span class="px-3 py-1 bg-[#00B050] text-white text-sm rounded-xl font-medium shadow-lg">Plataforma Web</span>
                </div>
            </div>
            <div class="p-6 flex flex-col flex-grow">
                <h3 class="text-lg font-bold text-white mb-3 flex-shrink-0">Track By Track</h3>
                <p class="text-white/60 text-sm leading-relaxed mb-4 flex-grow">A plataforma permite que os usuários avaliem músicas individualmente, calculem uma média para o álbum, registrem o histórico de escutas e compartilhem opiniões com a comunidade.</p>
                <a href="https://trackbytrackapp.com" target="_blank" rel="noopener noreferrer" class="inline-flex items-center text-[#00B050] hover:text-[#00B050]/80 font-medium text-sm transition-colors duration-300 mt-auto">
                    <span>Ver Projeto</span>
                    <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
                </a>
            </div>
        </div>
        <!-- Adicione os outros 5 cards estáticos aqui, exatamente como estavam antes -->
         <!-- Projeto 2: Conservatório -->
      <div class="group bg-white/5 backdrop-blur-sm rounded-2xl border border-white/10 shadow-2xl hover:shadow-blue-500/10 transition-all duration-500 hover:scale-105 hover:border-blue-500/30 overflow-hidden flex flex-col h-full">
        <div class="relative h-44 bg-blue-500/10 flex-shrink-0">
          <img src="/plano-marshall.webp" alt="Conservatório" class="w-full h-full object-cover opacity-80 group-hover:opacity-100 transition-opacity duration-300">
          <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-transparent"></div>
          <div class="absolute top-4 right-4">
            <span class="px-3 py-1 bg-blue-500 text-white text-sm rounded-xl font-medium shadow-lg">
              Website
            </span>
          </div>
        </div>
        <div class="p-6 flex flex-col flex-grow">
          <h3 class="text-lg font-bold text-white mb-3 flex-shrink-0">Conservatório</h3>
          <p class="text-white/60 text-sm leading-relaxed mb-4 flex-grow">
            Landing page moderna e informativa para o Conservatório de Música, com foco em design e na experiência do usuário.
          </p>
          <a href="https://conservatorioplanomarshall.com.br/" target="_blank" rel="noopener noreferrer" class="inline-flex items-center text-blue-400 hover:text-blue-400/80 font-medium text-sm transition-colors duration-300 mt-auto">
            <span>Ver Projeto</span>
            <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </a>
        </div>
      </div>

      <!-- Projeto 3: Odin -->
      <div class="group bg-white/5 backdrop-blur-sm rounded-2xl border border-white/10 shadow-2xl hover:shadow-purple-500/10 transition-all duration-500 hover:scale-105 hover:border-purple-500/30 overflow-hidden flex flex-col h-full">
        <div class="relative h-44 bg-purple-500/10 flex-shrink-0">
          <img src="/conservatorio.webp" alt="Odin Project" class="w-full h-full object-cover opacity-80 group-hover:opacity-100 transition-opacity duration-300">
          <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-transparent"></div>
          <div class="absolute top-4 right-4">
            <span class="px-3 py-1 bg-purple-500 text-white text-sm rounded-xl font-medium shadow-lg">
              Website
            </span>
          </div>
        </div>
        <div class="p-6 flex flex-col flex-grow">
          <h3 class="text-lg font-bold text-white mb-3 flex-shrink-0">Odin Project</h3>
          <p class="text-white/60 text-sm leading-relaxed mb-4 flex-grow">
            Website para o projeto Odin, com foco em design e na experiência do usuário.
          </p>
          <a href="#" class="inline-flex items-center text-purple-400 font-medium text-sm mt-auto">
            <span>Ver Projeto</span>
            <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </a>
        </div>
      </div>

      <!-- Projeto 4: ProFi -->
      <div class="group bg-white/5 backdrop-blur-sm rounded-2xl border border-white/10 shadow-2xl hover:shadow-orange-500/10 transition-all duration-500 hover:scale-105 hover:border-orange-500/30 overflow-hidden flex flex-col h-full">
        <div class="relative h-44 bg-orange-500/10 flex-shrink-0">
          <img src="/portfolio-joao.webp" alt="ProFi" class="w-full h-full object-cover opacity-80 group-hover:opacity-100 transition-opacity duration-300">
          <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-transparent"></div>
          <div class="absolute top-4 right-4">
            <span class="px-3 py-1 bg-orange-500 text-white text-sm rounded-xl font-medium shadow-lg">
              Website
            </span>
          </div>
        </div>
        <div class="p-6 flex flex-col flex-grow">
          <h3 class="text-lg font-bold text-white mb-3 flex-shrink-0">ProFi</h3>
          <p class="text-white/60 text-sm leading-relaxed mb-4 flex-grow">
            Portfólio para o profissional de finanças, com foco em design e na experiência do usuário.
          </p>
          <a href="#" class="inline-flex items-center text-orange-400 font-medium text-sm mt-auto">
            <span>Ver Projeto</span>
            <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </a>
        </div>
      </div>

      <!-- Projeto 5: Scratch -->
      <div class="group bg-white/5 backdrop-blur-sm rounded-2xl border border-white/10 shadow-2xl hover:shadow-cyan-500/10 transition-all duration-500 hover:scale-105 hover:border-cyan-500/30 overflow-hidden flex flex-col h-full">
        <div class="relative h-44 bg-cyan-500/10 flex-shrink-0">
          <img src="/qlook.webp" alt="Scratch Learning" class="w-full h-full object-cover opacity-80 group-hover:opacity-100 transition-opacity duration-300">
          <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-transparent"></div>
          <div class="absolute top-4 right-4">
            <span class="px-3 py-1 bg-cyan-500 text-white text-sm rounded-xl font-medium shadow-lg">
              Sistema de Gestão
            </span>
          </div>
        </div>
        <div class="p-6 flex flex-col flex-grow">
          <h3 class="text-lg font-bold text-white mb-3 flex-shrink-0">QLook Store</h3>
          <p class="text-white/60 text-sm leading-relaxed mb-4 flex-grow">
            Plataforma gamificada para ensino de programação para crianças e adolescentes com exercícios interativos.
          </p>
          <a href="#" class="inline-flex items-center text-cyan-400 font-medium text-sm mt-auto">
            <span>Ver Projeto</span>
            <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </a>
        </div>
      </div>

      <!-- CTA - Seu Projeto -->
      <div class="group bg-white/5 backdrop-blur-sm rounded-2xl border border-[#00B050]/30 shadow-2xl hover:shadow-[#00B050]/10 transition-all duration-500 hover:scale-105 hover:border-[#00B050]/50 overflow-hidden flex flex-col h-full">
        <div class="relative h-44 bg-white/5 flex-shrink-0 flex items-center justify-center">
          <div class="text-center">
            <div class="w-16 h-16 bg-white/10 backdrop-blur-sm rounded-2xl flex items-center justify-center mb-4 mx-auto border border-white/20">
              <svg class="w-8 h-8 text-[#00B050]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"/>
              </svg>
            </div>
            <span class="text-white/80 font-semibold text-lg">Seu Projeto Aqui</span>
          </div>
        </div>
        <div class="p-6 flex flex-col flex-grow text-center">
          <h3 class="text-lg font-bold text-white mb-3 flex-shrink-0">Vamos criar juntos?</h3>
          <p class="text-white/60 text-sm leading-relaxed mb-4 flex-grow">
            Que tal ser o próximo case de sucesso? Vamos transformar sua ideia em um projeto incrível e inovador.
          </p>
          <a href="#contact" class="inline-flex items-center justify-center bg-[#00B050] hover:bg-[#00B050]/90 text-white px-6 py-3 rounded-xl font-semibold text-sm transition-all duration-300 hover:scale-105 shadow-lg hover:shadow-[#00B050]/25 mt-auto">
            <span>Começar Projeto</span>
            <svg class="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const projects = [
        { imgSrc: "/trackbytrack.webp", tag: "Plataforma Web", tagColor: "#00B050", title: "Track By Track", description: "A plataforma permite que os usuários avaliem músicas individualmente, calculem uma média para o álbum, registrem o histórico de escutas e compartilhem opiniões com a comunidade.", link: "https://trackbytrackapp.com", hoverColor: "#00B050" },
        { imgSrc: "/plano-marshall.webp", tag: "Website", tagColor: "#3B82F6", title: "Conservatório", description: "Landing page moderna e informativa para o Conservatório de Música, com foco em design e na experiência do usuário.", link: "https://conservatorioplanomarshall.com.br/", hoverColor: "#3B82F6" },
        { imgSrc: "/conservatorio.webp", tag: "Website", tagColor: "#8B5CF6", title: "Odin Project", description: "Website para o projeto Odin, com foco em design e na experiência do usuário.", link: "#", hoverColor: "#8B5CF6" },
        { imgSrc: "/portfolio-joao.webp", tag: "Website", tagColor: "#F97316", title: "ProFi", description: "Portfólio para o profissional de finanças, com foco em design e na experiência do usuário.", link: "#", hoverColor: "#F97316" },
        { imgSrc: "/qlook.webp", tag: "Sistema de Gestão", tagColor: "#06B6D4", title: "QLook Store", description: "Plataforma gamificada para ensino de programação para crianças e adolescentes com exercícios interativos.", link: "#", hoverColor: "#06B6D4" },
        { type: 'cta', title: "Vamos criar juntos?", description: "Que tal ser o próximo case de sucesso? Vamos transformar sua ideia em um projeto incrível e inovador.", buttonText: "Começar Projeto", link: "#contact" }
    ];

    const carousel = document.getElementById('portfolio-carousel');
    const carouselInner = document.getElementById('portfolio-carousel-inner');
    const dotsContainer = document.getElementById('portfolio-carousel-dots');

    if (!carousel || !carouselInner || !dotsContainer) return;

    projects.forEach((project, index) => {
        const cardWrapper = document.createElement('div');
        cardWrapper.className = 'w-full flex-shrink-0 p-2';

        let cardHTML;
        if (project.type === 'cta') {
            cardHTML = `
            <div class="group bg-white/5 backdrop-blur-sm rounded-2xl border border-[#00B050]/30 shadow-2xl flex flex-col h-full p-6 text-center justify-center" style="min-height: 420px;">
                <div class="w-16 h-16 bg-white/10 rounded-2xl flex items-center justify-center mb-4 mx-auto border border-white/20">
                    <svg class="w-8 h-8 text-[#00B050]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"/></svg>
                </div>
                <h3 class="text-lg font-bold text-white mb-3">${project.title}</h3>
                <p class="text-white/60 text-sm leading-relaxed mb-4 flex-grow">${project.description}</p>
                <a href="${project.link}" class="inline-flex items-center justify-center bg-[#00B050] text-white px-6 py-3 rounded-xl font-semibold text-sm mt-auto">
                    <span>${project.buttonText}</span>
                    <svg class="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
                </a>
            </div>`;
        } else {
            cardHTML = `
            <div class="group bg-white/5 backdrop-blur-sm rounded-2xl border border-white/10 shadow-2xl overflow-hidden flex flex-col h-full" style="min-height: 420px;">
                <div class="relative h-44 bg-gray-800 flex-shrink-0">
                    <img src="${project.imgSrc}" alt="${project.title}" class="w-full h-full object-cover opacity-80">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-transparent"></div>
                    <div class="absolute top-4 right-4">
                        <span class="px-3 py-1 text-white text-sm rounded-xl font-medium shadow-lg" style="background-color: ${project.tagColor};">${project.tag}</span>
                    </div>
                </div>
                <div class="p-6 flex flex-col flex-grow">
                    <h3 class="text-lg font-bold text-white mb-3">${project.title}</h3>
                    <p class="text-white/60 text-sm leading-relaxed mb-4 flex-grow">${project.description}</p>
                    <a href="${project.link}" target="_blank" rel="noopener noreferrer" class="inline-flex items-center font-medium text-sm mt-auto" style="color: ${project.hoverColor};">
                        <span>Ver Projeto</span>
                        <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
                    </a>
                </div>
            </div>`;
        }
        cardWrapper.innerHTML = cardHTML;
        carouselInner.appendChild(cardWrapper);

        const dot = document.createElement('button');
        dot.className = `w-2 h-2 rounded-full bg-white/30 transition-colors ${index === 0 ? 'bg-white' : ''}`;
        dot.onclick = () => goToSlide(index);
        dotsContainer.appendChild(dot);
    });

    let currentSlide = 0;
    const totalSlides = projects.length;

    function updateCarouselWidth() {
        const slideWidth = carousel.offsetWidth;
        carouselInner.style.width = `${slideWidth * totalSlides}px`;
        Array.from(carouselInner.children).forEach(slide => {
            (slide as HTMLElement).style.width = `${slideWidth}px`;
        });
        goToSlide(currentSlide);
    }

    function goToSlide(index) {
        currentSlide = index;
        const offset = -index * carousel.offsetWidth;
        carouselInner.style.transform = `translateX(${offset}px)`;
        Array.from(dotsContainer.children).forEach((dot, i) => {
            dot.classList.toggle('bg-white', i === index);
            dot.classList.toggle('bg-white/30', i !== index);
        });
    }

    let touchStartX = 0;
    carousel.addEventListener('touchstart', (e) => { touchStartX = e.touches[0].clientX; });
    carousel.addEventListener('touchend', (e) => {
        const touchEndX = e.changedTouches[0].clientX;
        const diff = touchStartX - touchEndX;
        if (Math.abs(diff) > 50) {
            goToSlide((currentSlide + (diff > 0 ? 1 : -1) + totalSlides) % totalSlides);
        }
    });

    window.addEventListener('resize', updateCarouselWidth);
    updateCarouselWidth();
  });
</script>

<style>
  .floating-element { animation: float 6s ease-in-out infinite; }
  .floating-element:nth-child(2) { animation-delay: -2s; }
  .floating-element:nth-child(3) { animation-delay: -4s; }
  .floating-element:nth-child(4) { animation-delay: -6s; }
  @keyframes float { 0%, 100% { transform: translateY(0px); } 50% { transform: translateY(-20px); } }
</style>